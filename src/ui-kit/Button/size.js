import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  buttonStyes: {
    justifyContent: 'center',
    alignItems: 'center',
    borderWidth: 1,
    borderRadius: 12,
  },
  small: {
    width: 80,
    height: 40,
  },
});
