import { StyleSheet } from 'react-native';
import Colors from '../../../utils/colors';

export default StyleSheet.create({
  textstyle: {
    ineHeight: 24,
    color: Colors.white,
  },
  small: { fontSize: 14 },
  medium: { fontSize: 16 },
});
