import { StyleSheet } from 'react-native';
import Colors from '../../../utils/colors';

export default StyleSheet.create({
  small: { fontSize: 14, lineHeight: 24, color: Colors.white },
  medium: { fontSize: 16, ineHeight: 24, color: Colors.white },
  large: {},
});
